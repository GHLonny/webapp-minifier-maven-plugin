<project>
   <build>
      <plugins>
         <plugin>
            <artifactId>webapp-minifier-maven-plugin</artifactId>
            <configuration>
               <outputDirectory>${basedir}/target/test-yui-min</outputDirectory>
            </configuration>
         </plugin>
      </plugins>
   </build>
</project>
