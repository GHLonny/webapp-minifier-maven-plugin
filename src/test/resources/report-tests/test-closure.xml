<project>
   <build>
      <plugins>
         <plugin>
            <artifactId>webapp-minifier-maven-plugin</artifactId>
            <configuration>
               <outputDirectory>${basedir}/target/test-closure-min</outputDirectory>
            </configuration>
         </plugin>
      </plugins>
   </build>
</project>
