<project>
   <build>
      <plugins>
         <plugin>
            <artifactId>webapp-minifier-maven-plugin</artifactId>
            <configuration>
               <minifiedDirectory>${basedir}/target/test-closure-min</minifiedDirectory>
               <outputDirectory>${basedir}/target/site</outputDirectory>
            </configuration>
         </plugin>
      </plugins>
   </build>
</project>
